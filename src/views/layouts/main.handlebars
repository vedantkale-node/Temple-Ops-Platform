<!DOCTYPE html>
<html lang="en" class="h-full overflow-hidden bg-white dark:bg-gray-950">
{{> header}}

<body class="bg-white text-slate-900 dark:bg-gray-950 dark:text-slate-100" data-success="{{success}}"
    data-error="{{errors}}">
    {{{body}}}
</body>
{{> scripts}}

</html>